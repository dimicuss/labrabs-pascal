program L8zad4;

uses SysUtils;

function Fib(n : integer ) : integer;
begin
   if (n = 1) or (n = 2) then Fib := 1
   else if n > 2 then Fib := Fib(n - 2) + Fib(n - 1);
end; { Fib }

begin
   writeln(Fib(4));
end.

